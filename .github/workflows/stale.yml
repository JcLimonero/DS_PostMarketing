name: Close inactive issues

# Scheduled runs are disabled on this fork: the job below is guarded to
# gitroomhq/postiz-app, so every scheduled run here queued a runner only to
# skip. Restore the `schedule:` block if this ever runs on the upstream repo.
#
#  schedule:
#    - cron: "*/30 * * * *"
on:
  workflow_dispatch:

jobs:
  close-issues:
    if: github.repository == 'gitroomhq/postiz-app'
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
      - uses: actions/stale@v9
        with:
          days-before-issue-stale: 90
          days-before-issue-close: 7
          stale-issue-label: "stale"
          stale-issue-message: "This issue is stale because it has been open for 90 days with no activity."
          close-issue-message: "This issue was closed because it has been inactive for 7 days since being marked as stale." 
          exempt-issue-labels: "no-stale-bot"
          
          days-before-pr-stale: 90
          days-before-pr-close: 7
          stale-pr-label: "stale" 
          stale-pr-message: "This PR is stale because it has been open for 90 days with no activity."
          close-pr-message: "This PR was closed because it has been inactive for 7 days since being marked as stale." 
          exempt-pr-label: "no-stale-bot" 
          
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          operations-per-run: 180
